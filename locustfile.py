from src.performance.tasks.simple_conversation_flow import ChannelConversationUser

# (optional) set a default host so you don't have to pass --host on the CLI
#ChannelConversationUser.host = "https://stage.hoory.com"
